# giterloper_test_knowledge

This repository is a test-only giterloper knowledge store used for end-to-end validation in this project.

The store intentionally contains a minimal `knowledge/` directory and can be populated by tests that verify the
`gl` write/read workflows.
